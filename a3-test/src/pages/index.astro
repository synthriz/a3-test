---


// pagina inicial ;)
import "../styles/global.css";
---

<html lang="pt-BR">
	<head>
		<meta charset="UTF-8" />
		<title>A3 Media - Teste Técnico</title>
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	</head>
	<body
		class="min-h-screen
		bg-[radial-gradient(circle_at_top_left,_#0d1f1a,_#2e8f74,_#42d59c)]

		text-white flex flex-col items-center justify-center px-4 py-10"
	>
		<main
			class="max-w-3xl w-full bg-white bg-opacity-10 rounded-2xl shadow-xl backdrop-blur-lg p-8 space-y-8 text-center text-black"
		>
		<!-- título -->
			<h1 class="text-3xl md:text-4xl font-bold">
				🧪 A3 Media - Teste Técnico
			</h1>
			<!-- texto principal -->
			<p>
				Esse projeto replica duas páginas de funil utilizando
				<a
					href="https://astro.build/"
					target="_blank"
					class="font-bold text-black hover:text-[#42d59c] transition-colors duration-300"
				>
					Astro
				</a>
				+
				<a
					href="https://tina.io/"
					target="_blank"
					class="font-bold text-black hover:text-[#42d59c] transition-colors duration-300"
				>
					TinaCMS
				</a>. As páginas foram clonadas 1000 vezes com conteúdo dinâmico
				via
				<a href="https://fakerjs.dev/"
				target="_blank" 
				class="text-black hover:text-[#42d59c] transition-colors duration-300">
					<code class="bg-black/10 px-1 rounded text-sm">faker-js</code>
				</a>
				, conforme o desafio proposto.
			</p>
			<!-- acessar template 1 -->
			<section class="space-y-6">
				<div>
					<h2 class="text-2xl font-semibold mb-2">
						🔗 Acesse a Página Template 1
					</h2>
					<form
						id="form1"
						class="flex flex-col sm:flex-row items-center justify-center gap-4"
					>
						<input
							id="input1"
							type="number"
							min="1"
							max="1000"
							class="rounded-xl border-2 border-[#42d59c] px-4 py-2 text-black text-lg w-40"
							placeholder="1 a 1000"
							required
						/>
						<button
							type="submit"
							class="px-4 py-2 rounded-xl bg-white text-[#0d1f1a] font-semibold hover:bg-[#42d59c] transition"
						>
							Ir para página
						</button>
					</form>
				</div>

				<!-- acessar template 2 -->
				<div>
					<h2 class="text-2xl font-semibold mb-2">
						🔗 Acesse a Página Template 2
					</h2>
					<form
						id="form2"
						class="flex flex-col sm:flex-row items-center justify-center gap-4"
					>
						<input
							id="input2"
							type="number"
							min="1"
							max="1000"
							class="rounded-xl border-2 border-[#42d59c] px-4 py-2 text-black text-lg w-40"
							placeholder="1 a 1000"
							required
						/>
						<button
							type="submit"
							class="px-4 py-2 rounded-xl bg-white text-[#0d1f1a] font-semibold hover:bg-[#42d59c] transition"
						>
							Ir para página
						</button>
					</form>
				</div>
			</section>

			<!-- footer <3 -->
			<footer class="pt-6 text-black/80 text-sm">
				<a href="https://www.linkedin.com/in/trizbeatavares/"
				target="_blank" 
				class="text-black hover:text-[#42d59c] transition-colors duration-300">
					<span>
						Feito com 💚 por Beatriz Tavares
					</span>
				</a>
			</footer>
		</main>

		<script>
			// seleciona o formulario por id "form1"
			// se encontrar, adiciona ouvinte de evento (eventListener) pro envio (submit)
			
			document
				.getElementById("form1")
				?.addEventListener("submit", function (e) {
					// e.preventDefault() => pra previnir que dê reload na pagina
					e.preventDefault();
					// pega o campo com id "input1"
					const input = document.getElementById(
						"input1",
					) as HTMLInputElement;
					// transforma o valor digitado (uma string) em int
					const page = parseInt(input.value);
					// verifica se ele esta entre 1 e 1k
					// se sim, abre a janela
					// senao, mostra alerta que o valor é inválido
					if (page >= 1 && page <= 1000) {
						window.open(`/paginatemplate1/page-${page}`, "_blank");
					} else {
						alert("Insira um número entre 1 e 1000 ;)");
					}
				});

			document
				.getElementById("form2")
				?.addEventListener("submit", function (e) {
					e.preventDefault();
					const input = document.getElementById(
						"input2",
					) as HTMLInputElement;
					const page = parseInt(input.value);
					if (page >= 1 && page <= 1000) {
						window.open(`/paginatemplate2/page-${page}`, "_blank");
					} else {
						alert("Insira um número entre 1 e 1000 ;)");
					}
				});
		</script>
	</body>
</html>
